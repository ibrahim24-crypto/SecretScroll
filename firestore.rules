rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
  
    function isOwner(userId) {
      return request.auth != null && request.auth.uid == userId;
    }

    function isAdmin() {
      return request.auth != null && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
  
    // Users can only read public profiles, but can write to their own. Admins can also update profiles.
    match /users/{userId} {
      allow read: if true;
      allow create: if isOwner(userId);
      allow update: if isOwner(userId) || isAdmin();
    }

    // Posts can be read by anyone and created by anyone.
    // Only the author (if logged in) or an admin can update or delete a post.
    match /posts/{postId} {
      allow read: if true;
      allow create: if true;
      allow update: if isOwner(resource.data.authorUid) || isAdmin();
      allow delete: if isOwner(resource.data.authorUid) || isAdmin();
    }
    
    // Votes can be created, updated, or deleted by the user who owns them.
    match /votes/{voteId} {
      allow read, write: if isOwner(request.resource.data.userId);
    }

    match /comments/{commentId} {
      allow read: if true;
      // Anyone can create a comment, as long as it's not empty and reasonably short.
      allow create: if request.resource.data.content != null 
                    && request.resource.data.content.size() > 0
                    && request.resource.data.content.size() < 500;
      // Only the comment author or an admin can delete a comment.
      allow delete: if isOwner(resource.data.userId) || isAdmin();
    }
  }
}
